{
  "source": "30_days_of_python",
  "day": 18,
  "module": "regular_expressions",
  "exercise_type": "function",
  "id": "30days-d18-l1-02",
  "level": 1,
  "difficulty": 2,
  "title": "Extract numbers and find the furthest distance",
  "description": "Write a function `max_distance(text)` that extracts every integer (including negative ones) from `text` using a regular expression, and returns the distance between the two furthest-apart values.",
  "examples": "max_distance('positions: -12, -4, -3, -1, 0, 4, 8')\n-> 20",
  "starter_code": "import re\n\n\ndef max_distance(text):\n    # your code here\n    pass\n",
  "hints": [
    "Pattern to consider: -?\\d+",
    "The `-?` makes the leading minus sign optional in the match, so the same pattern matches both '8' and '-12' - without it, the minus sign would be dropped and every number would come out positive.",
    "Use re.findall() with that pattern to pull out every number as a string, convert each to int, and combine the built-in min() and max() functions to get the distance between the two furthest values."
  ],
  "expected_behavior": "Returns the difference between the largest and smallest number found in text.",
  "hidden_tests": [
    {
      "call": "max_distance('positions: -12, -4, -3, -1, 0, 4, 8')",
      "expected": "20",
      "label": "source item 2"
    }
  ],
  "solution": "import re\n\n\ndef max_distance(text):\n    numbers = [int(n) for n in re.findall(r\"-?\\d+\", text)]\n    return max(numbers) - min(numbers)\n",
  "explanation": "The optional `-?` in the pattern is what makes negative numbers extract correctly - without it, re.findall would match only the digits and silently drop every minus sign.",
  "concepts": [
    "regular expressions",
    "re.findall"
  ],
  "skills": [
    "text_processing"
  ]
}
